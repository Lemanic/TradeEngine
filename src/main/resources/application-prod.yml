spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trade_engine_db
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
app:
  strategies:
    - name: DOUBLE_DIVERGENCE_STRATEGY
      enabled: true
      timeframes: [ H1, H4, D1 ]
  telegram:
    bot-token: ${TELEGRAM_BOT_TOKEN}
    chat-id: ${TELEGRAM_CHAT_ID}